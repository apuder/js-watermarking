
var root = {};

function trivial() {
	root.current = {}
if (root.current && !root.current.last) {
	root.current.last = {};
}
if (root.current && root.current.last && !root.current.last.self) {
	root.current.last.self = {};
}
if (root.current && root.current.last && root.current.last.self && !root.current.last.self[0] && root.current) {
	root.current.last.self[0] = root.current;
}
if (root.current && !root.current.prev && root.current && root.current.last && root.current.last.self) {
	root.current.prev = root.current.last.self;
}
if (root.current && root.current.last && !root.current.last.heap && root.current && root.current.prev) {
	root.current.last.heap = root.current.prev;
}

}

trivial();




